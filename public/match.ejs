<!-- FILEPATH: /c:/Users/New User/OneDrive/Documents/CMSC335/matcha/public/match.ejs -->
<!-- BEGIN: be15d9bcejpp -->
<!DOCTYPE html>
<html>
<head>
    <title>Bubble Tea Information</title>
    <link rel="stylesheet" href="/css/match.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Modak&display=swap" rel="stylesheet">
    
</head>
<body>
    <h1>find ur perfect tea</h1>
    <!-- BEGIN: Loop through data and display cards -->
    <% data.forEach(function(item) { %>
        <div class="card">
            <h2>Bubble Tea </h2>
            <p><%= item.bubbleTea %></p>
            <h2>Toppings </h2>
            <p><%= item.toppings.join(', ') %></p>
            <h2>Fun fact</h2>
            <p><%= item.fact %></p>
        </div>
    <% }); %>
    <!-- END: Loop through data and display cards -->

    <button class="bubbleTeaButtons swipe-button-left"> swipe left </button>
    <button class="bubbleTeaButtons swipe-button swipe-button-right"> swipe right </button>


    <script>
        let cards = Array.from(document.querySelectorAll('.card'));

        let currentIndex = 0;

        document.querySelector('.swipe-button-left').addEventListener('click', function() {
            cards[currentIndex].style.display = 'none';
            currentIndex = (currentIndex - 1 + cards.length) % cards.length;
            cards[currentIndex].style.display = 'block';
        });

        document.querySelector('.swipe-button-right').addEventListener('click', function() {
            cards[currentIndex].style.display = 'none';
            currentIndex = (currentIndex + 1) % cards.length;
            cards[currentIndex].style.display = 'block';
        });
    </script>
</body>
</html>
<!-- END: be15d9bcejpp -->
